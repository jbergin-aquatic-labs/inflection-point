{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-mcp-server",
      "type": "shell",
      "command": "npm",
      "args": ["run", "build"],
      "options": {
        "cwd": "${workspaceFolder}/mcp_server"
      },
      "group": "build",
      "problemMatcher": ["$tsc"]
    }
  ]
}
