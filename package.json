{
  "name": "inflection-point-monorepo",
  "private": true,
  "description": "Inflection Point: VS Code debug bridge + TypeScript MCP server",
  "workspaces": ["mcp_server", "vscode_extension"],
  "scripts": {
    "build": "npm run build -w mcp_server && npm run bundle:server-for-extension && npm run build -w vscode_extension && npm run vsix -w vscode_extension",
    "build:server": "npm run build -w mcp_server",
    "build:extension": "npm run build -w vscode_extension",
    "bundle:server-for-extension": "node scripts/bundle_mcp_for_extension.mjs",
    "start:server": "npm run start -w mcp_server",
    "test": "npm test -w mcp_server && npm test -w vscode_extension"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
